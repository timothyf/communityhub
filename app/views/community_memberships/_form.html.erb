<%= form_with(model: community_membership, local: true) do |form| %>
  <% if community_membership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community_membership.errors.count, "error") %> prohibited this community_membership from being saved:</h2>

      <ul>
      <% community_membership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
