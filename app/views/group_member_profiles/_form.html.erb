<%= form_with(model: group_member_profile, local: true) do |form| %>
  <% if group_member_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group_member_profile.errors.count, "error") %> prohibited this group_member_profile from being saved:</h2>

      <ul>
      <% group_member_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
